TEXT MESSAGE SPAM FILTERING USIG NAIVE BAYES CLASSIFIER

*numpy and matplotlib will be required to run the code*

RUNNING INSTRUCTIONS : 

1) Place all the files in a desired location

2) In the python shell, excecute the two .py files in order as follows 
	
	>>python Spam_Classifier.py
	>>python Alpha_Moderator.py

3) Output for 4-a can be seen on the terminal (for both training and test data)
   Output for 4-b will show up as graphs in a single window. Maximize the window for better visualization
